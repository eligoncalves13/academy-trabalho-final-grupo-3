{
    "auth": "#(response.auth)",
    "session": {
        "token": "#(response.session.token)",
        "issued": "#(response.session.issued)",
        "expires": "#(response.session.expires)"
    }
}